<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#6ED371"
    android:layout_centerVertical="true"
    android:layout_centerHorizontal="true">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:gravity="center"
        android:orientation="vertical"
        android:layout_centerHorizontal="true"
        >
        <EditText
            android:id="@+id/main_EDT_playerName"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:background="@color/white"
            android:textColorHint="@color/black"
            android:text="Your name"
            android:textSize="60dp"
            android:layout_marginBottom="100dp"
            android:layout_gravity="center"
            android:textEditPasteWindowLayout="@color/black"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/main_BTN_play"
            android:layout_width="300dp"
            android:layout_height="100dp"
            android:layout_marginEnd="40dp"
            android:backgroundTint="#0F8013"
            android:text="Play"
            android:textAlignment="center"
            android:textSize="60dp"
            app:cornerRadius="20dp"
            app:icon="@drawable/ic_play"
            app:iconSize="75dp"
            app:iconTint="@android:color/transparent"
            app:iconTintMode="add"
            android:layout_marginBottom="100dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/main_BTN_topTen"
            android:layout_width="400dp"
            android:layout_height="100dp"
            android:layout_marginEnd="40dp"
            android:backgroundTint="#0F8013"
            android:text="Top Ten"
            android:textAlignment="textStart"
            android:textSize="60dp"
            app:icon="@drawable/ic_top_ten"
            app:iconSize="70dp"
            app:iconTint="@android:color/transparent"
            app:iconTintMode="add"/>

    </LinearLayout>

</RelativeLayout>